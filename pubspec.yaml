name: flutter_qq
description: A new Flutter project.

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
version: 1.0.0+1

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^0.1.2
  flutter_webview_plugin: "^0.2.1+2"
  date_format: "^1.0.4"
  dio: ^3.0.4
  protobuf: ^0.14.4
  sso_request_engine: ^0.0.1
dev_dependencies:
  flutter_test:
    sdk: flutter


# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  assets:
  - images/loading.png

  #4个tab切换图片资源
  - images/tab_qworld_nor.png
  - images/tab_qworld_press.png
  - images/tab_see_nor.png
  - images/tab_see_press.png
  - images/tab_buddy_nor.png
  - images/tab_buddy_press.png
  - images/tab_recent_nor.png
  - images/tab_recent_press.png

  #菜单按钮
  - images/icon_menu_addfriend.png
  - images/icon_menu_group.png
  - images/icon_menu_scan.png
  
  #消息页
  - images/tab_recent_camera.png
  - images/right_menu_nor.png

  # #联系人图标
  - images/nav_icon_add_friend.png

  #动态模块图标
  - images/nav_icon_setting.png
  - images/dynamic_qzonefriend.png
  - images/dynamic_nearby.png
  - images/dynamic_grouptribe.png
  - images/dynamic_kuolie.png
  - images/dynamic_game.png
  - images/dynamic_read.png
  - images/dynamic_story.png
  - images/dynamic_gouwu.png
  - images/dynamic_comic.png
  - images/dynamic_nowlive.png
  - images/dynamic_qegame.png
  - images/dynamic_music.png
  - images/dynamic_sayhonest.png
  - images/dynamic_health.png
  - images/dynamic_news.png
  - images/dynamic_ketang.png
  - images/dynamic_qqtutor.png

  # To add assets to your application, add an assets section, like this:
  # assets:
  #  - images/a_dot_burr.jpeg
  #  - images/a_dot_ham.jpeg

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/assets-and-images/#resolution-aware.

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/assets-and-images/#from-packages

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/custom-fonts/#from-packages
