import 'package:flutter/material.dart';

class FriendSectionPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return new Center(
      child: new Text(
        "分组",
        style: new TextStyle(fontSize: 20.0),
      ),
    );
  }
}
